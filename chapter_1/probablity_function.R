# d****(... ) 확률분포 (density)
# p****(... ) 누적활률 (probability)
# q****(... ) 분위수 (quantile)
# r****(n, ... ) 확률변수(난수) (random number)
# d, p, q, r 뒤에 따르는 확률분포 이름 (이 외에도 많이 있음)
# norm(x, m=0, sd=1) 정규 분포
# t(x, df) t-분포
# chisq(x, df) 카이제곱분포
# f(x, df1, df2) F-분포
# binom(x, size, prob) 이항분포
# pois(x, lambda) 포아송분포
# unif(x, min=0, max=1) 균일 분포