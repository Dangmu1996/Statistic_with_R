a = 4
b = 3

a <- b # b를 a에 할당
a = b #b를 a에 할당(<- 연산자에 비해 우선순위에서 밀림)
a <<- b #b를 a에 할당
a -> b #a를 b에 할당
a ->> b #a를 b에 할당

a+b #a와 b를 더함
a-b #a에서 b를 뺌
a*b #a와 b를 곱함
a/b #a를 b로 나눈 나눔
a%%b #a를 b로 나눈 나머지
a%/%b #a를 b로 나눈 몫
a^b #a를 b번 곱함 (a의 b승)

